<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- allowBackup=false + dataExtractionRules: WebView-lagringen skal BLI på
         enheten. Der ligger Supabase-sesjonen (med refresh-tokenet, som fornyer
         seg selv) og den lokale bufferen med alt innholdet — i klartekst, som i
         nettleseren. Med Androids standard `true` følger begge med i
         sikkerhetskopien, og en gjenoppretting på en annen telefon er innlogget
         som brukeren uten at noen har logget inn. Nettleserutgaven har ingen
         slik vei ut av enheten, og appen skal ikke ha en.

         Ingenting går tapt av å slå det av: serveren er kanonisk, så en ny
         telefon henter alt ved første innlogging.

         Regelfila trengs I TILLEGG fordi `allowBackup=false` bare dekker
         skykopien: på Android 12+ (targetSdk 36) fortsetter enkelte
         produsenters enhet-til-enhet-overføring uansett. Begrunnelsen i sin
         helhet: docs/mobilapp-plan.md, «Sikker lagring og sikkerhetskopi». -->
    <application
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!-- windowSoftInputMode=adjustResize: skjermtastaturet skal KRYMPE
             vinduet, ikke skyve det opp. Web-laget regner med at et synlig
             tastatur gir mindre viewport (resize-lytteren i app.js ruller
             feltet som redigeres tilbake i syne). Uten erklæringen står
             modusen på «unspecified», og hvilken av de to systemet velger er
             ikke vår avgjørelse. -->
        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:windowSoftInputMode="adjustResize"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>
    </application>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />
</manifest>
